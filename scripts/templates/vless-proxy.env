# Optional VLESS proxy env for bot + cliproxy.
# Keep backend/internal endpoints out of proxy path.

NODE_USE_ENV_PROXY=1
HTTP_PROXY=http://vless-proxy:8080
HTTPS_PROXY=http://vless-proxy:8080
ALL_PROXY=http://vless-proxy:8080
NO_PROXY=backend,localhost,127.0.0.1,opencode,cliproxy,vless-proxy
