http:
  middlewares:
    opencode-forward-auth:
      forwardAuth:
        address: "http://bot:3001/opencode-auth/check"
        trustForwardHeader: false
        authResponseHeaders:
          - "X-Auth-Admin-ID"
